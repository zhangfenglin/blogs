---
description: 博客仓库结构、中文命名与 Agent 行为底线
alwaysApply: true
---

# 博客仓库约定

- 本仓库为中文**技术分享**博客；目录与文章文件名使用中文（可含序号与必要英文专有名词）。
- 专栏目录为可选组织方式，不强制文章归入特定专栏。
- **单篇 / 系列判定**以 `博客写作.mdc` 的**场景路由**为准；目录锚点优先于 frontmatter。
- **单篇**：独立 Markdown；frontmatter 无 `系列` 字段，使用 `单篇篇型`。
- **系列**：同目录共享 `系列` frontmatter；导读锚点为 `00-系列导读.md`，或 `README.md`（frontmatter `角色: 系列导读`）。
- 系列可仅含导读 + 深度篇，**不强制**索引篇。
- 独立系列目录可放仓库根或任意中文目录。
- 增删改文章后，同步更新根 `README.md`（不存在则创建）及所在目录索引。
- 新建目录优先中文名。
- 写作规范见 `博客写作.mdc`（单文件，alwaysApply）。
